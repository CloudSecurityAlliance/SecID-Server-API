fastapi>=0.115.0
uvicorn>=0.34.0
# Optional backends (uncomment as needed):
# redis>=5.0.0
# pymemcache>=4.0.0
# Optional MCP server (uncomment to enable /mcp endpoint).
# Works with mcp 1.x (FastMCP) and 2.x (MCPServer):
# mcp>=1.8.0
