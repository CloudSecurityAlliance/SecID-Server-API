# TypeScript Implementation

Planned. Will extract the portable resolver logic from [SecID-Service](https://github.com/CloudSecurityAlliance/SecID-Service) (`parser.ts`, `resolver.ts`) and add file-based and Redis storage backends.

The goal is a server that's as close to the production Cloudflare Worker as possible, but runs anywhere with Node.js.
